---
// src/pages/tools/svg-to-jsx.astro
import Layout from '../../layouts/Layout.astro';
import SvgTool from '../../components/tools/SvgToJsx';
import RelatedTools from '../../components/RelatedTools.astro';

const title = "SVG to JSX Converter | SyntaxSnap";
const description = "Instantly convert SVG code into clean React JSX. Automatically handles camelCase attributes, className updates, and style objects.";

// Schema Data
const schema = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "SoftwareApplication",
      "name": "SVG to JSX Converter",
      "operatingSystem": "Web",
      "applicationCategory": "DeveloperApplication",
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "USD"
      },
      "description": description,
      "featureList": [
        "Converts kebab-case attributes to camelCase",
        "Transforms style strings to objects",
        "Fixes class to className",
        "Local-first privacy (no server upload)"
      ]
    },
    {
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "Is my SVG data sent to a server?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "No. SyntaxSnap is a local-first platform. All processing happens locally in your browser using JavaScript. Your code never leaves your device."
          }
        },
        {
          "@type": "Question",
          "name": "Does this tool fix style attributes?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Yes. It automatically parses CSS strings in the style attribute and converts them into a React-compatible JavaScript object."
          }
        },
        {
          "@type": "Question",
          "name": "Is this tool free to use?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "Yes, this SVG to JSX converter is 100% free and open for all developers."
          }
        }
      ]
    }
  ]
};
---

<Layout title={title} description={description} isTool={true}>
  
  <script type="application/ld+json" set:html={JSON.stringify(schema)} />

  <div class="max-w-6xl mx-auto">
    <div class="mb-10">
      <h1 class="text-4xl font-extrabold text-white mb-4">SVG to JSX Transformer</h1>
      <p class="text-slate-400 text-lg">
        Paste your raw SVG code to get a React-compatible component instantly.
      </p>
    </div>

    <SvgTool client:idle />
    
    <div class="mt-16 grid grid-cols-1 md:grid-cols-2 gap-12">
      
      <div class="bg-slate-900/30 border border-slate-800 rounded-2xl p-8">
        <h2 class="text-xl font-bold text-white mb-4">Why use this converter?</h2>
        <p class="text-slate-400 mb-4 leading-relaxed">
          React uses a synthetic event system and expects HTML attributes to be in <code class="text-indigo-400">camelCase</code>. 
          Manually converting large SVGs is tedious and error-prone.
        </p>
        <ul class="space-y-2 text-sm text-slate-400 marker:text-indigo-500 list-disc pl-4">
          <li>Changes <code class="text-slate-300">stroke-width</code> to <code class="text-indigo-300">strokeWidth</code></li>
          <li>Changes <code class="text-slate-300">class</code> to <code class="text-indigo-300">className</code></li>
          <li>Converts style strings to JSON objects</li>
          <li>Removes XML headers and comments</li>
        </ul>
      </div>

      <div>
        <h2 class="text-xl font-bold text-white mb-6">Frequently Asked Questions</h2>
        <div class="space-y-6">
          
          <div>
            <h3 class="font-medium text-slate-200 mb-2">Is my SVG data sent to a server?</h3>
            <p class="text-sm text-slate-400 leading-relaxed">
              No. SyntaxSnap is a local-first platform. All processing happens locally in your browser using JavaScript. Your code never leaves your device.
            </p>
          </div>

          <div>
            <h3 class="font-medium text-slate-200 mb-2">Does this tool fix style attributes?</h3>
            <p class="text-sm text-slate-400 leading-relaxed">
              Yes. It automatically parses CSS strings in the style attribute and converts them into a React-compatible JavaScript object.
            </p>
          </div>

          <div>
            <h3 class="font-medium text-slate-200 mb-2">Is this tool free to use?</h3>
            <p class="text-sm text-slate-400 leading-relaxed">
              Yes, this SVG to JSX converter is 100% free and open for all developers.
            </p>
          </div>

        </div>
      </div>
     <RelatedTools currentTool="svg-to-jsx" />  
    </div>
  </div>
</Layout>