---
// src/components/ui/RelatedTools.astro
import { TOOLS, type Tool } from '../../config/tools';

interface Props {
  currentTool: string;
}

const { currentTool } = Astro.props;

// Filter out the current tool
const relatedTools = TOOLS.filter((tool: Tool) => tool.id !== currentTool);

// Logic to find tools in the same category
const currentToolData = TOOLS.find((t: Tool) => t.id === currentTool);
const sameCategory = relatedTools.filter((t: Tool) => t.category === currentToolData?.category);

// Determine which 3 tools to display
const displayTools = sameCategory.length >= 3 
  ? sameCategory.slice(0, 3) 
  : relatedTools.slice(0, 3);
---

<section class="border-t border-slate-800 mt-20 pt-16 mb-12">
  <div class="text-center mb-12 px-4">
    <h2 class="text-2xl font-bold text-white mb-3 tracking-tight">Explore More Developer Tools</h2>
    <p class="text-slate-400">Boost your productivity with our other privacy-first utilities.</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-6xl mx-auto px-4">
    {displayTools.map((tool) => (
      <a 
        href={tool.href}
        class="group p-6 rounded-2xl bg-slate-900/50 border border-slate-800 hover:border-indigo-500/50 hover:bg-slate-900 transition-all duration-300 flex flex-col items-start shadow-sm"
      >
        <div class={`p-3 rounded-xl ${tool.bg} ${tool.color} mb-4 group-hover:scale-110 transition-transform`}>
          <tool.icon class="w-6 h-6" />
        </div>
        <h3 class="text-lg font-bold text-white mb-2 group-hover:text-indigo-400 transition-colors">
          {tool.title}
        </h3>
        <p class="text-sm text-slate-400 leading-relaxed">
          {tool.desc}
        </p>
      </a>
    ))}
  </div>
</section>