---
// src/components/ui/Header.astro
import { Wrench, Info, Mail, Sparkles } from 'lucide-astro';
import Logo from './Logo.astro';
import { TOOLS } from '../../config/tools';

// Dynamically find the AI tool so the link never breaks
const aiTool = TOOLS.find(t => t.category === 'ai') || TOOLS[0];
---

<header class="border-b border-slate-800 bg-slate-950/80 backdrop-blur-md sticky top-0 z-50">
  <div class="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
    
    <a href="/" class="flex items-center gap-3 group" aria-label="SyntaxSnap home">
      <div class="group-hover:rotate-12 transition-transform duration-300">
         <Logo class="w-8 h-8" />
      </div>
      <span class="text-xl font-bold tracking-tight text-white">
        Syntax<span class="text-indigo-400">Snap</span>
      </span>
    </a>

    <nav aria-label="Main navigation">
      <ul class="flex gap-4 md:gap-8 text-base font-medium text-slate-400 list-none m-0 p-0">
        <li>
          {/* Automatically links to your AI tool from config */}
          <a href={aiTool.href} class="text-emerald-400 hover:text-emerald-300 transition-colors flex items-center gap-1.5 py-2" aria-label="Try AI Tools">
            <Sparkles class="w-4 h-4" /> <span class="hidden sm:inline">AI Tools</span>
          </a>
        </li>
        <li>
          <a href="/#all-tools" class="hover:text-white transition-colors flex items-center gap-1.5 py-2" aria-label="View Tools">
            <Wrench class="w-4 h-4" /> <span class="hidden sm:inline">Tools</span>
          </a>
        </li>
        <li>
          <a href="/about" class="hover:text-white transition-colors flex items-center gap-1.5 py-2" aria-label="About SyntaxSnap">
            <Info class="w-4 h-4" /> <span class="hidden sm:inline">About</span>
          </a>
        </li>
        <li>
          <a href="/contact" class="hover:text-white transition-colors flex items-center gap-1.5 py-2" aria-label="Contact Support">
            <Mail class="w-4 h-4" /> <span class="hidden sm:inline">Contact</span>
          </a>
        </li>
      </ul>
    </nav>
  </div>
</header>